//nao esta completo

const {gets, print} = require('./funcoes03');

let maiorValorEncontrado = 0;
const quantida = gets();

const menorValor = Math.min(quantida);
const maior = Math.max(quantida)

print(maior);
print(menorValor);

